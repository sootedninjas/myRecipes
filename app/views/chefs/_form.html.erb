<%= render 'shared/errors', obj: @chef %>

<div class="row">
  <div class="well cold-md-8 col-md-offset-2">
    <%= form_for @chef do |f| %>
      <%= f.label :chefname %>
      <%= f.text_field :chefname %>
      
      <%= f.label :email %>
      <%= f.email_field :email %>
      
      <%= f.label :password %>
      <%= f.password_field :password %>
      
      <br>
      <%= f.submit(@chef.new_record? ? 'Register' : 'Edit Profile', 
            class: "btn btn-success") %>
    <% end %>
  </div>
</div>